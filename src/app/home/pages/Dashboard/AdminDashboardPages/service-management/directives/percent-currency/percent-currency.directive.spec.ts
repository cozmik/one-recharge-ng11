import { PercentCurrencyDirective } from './percent-currency.directive';

describe('PercentCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
