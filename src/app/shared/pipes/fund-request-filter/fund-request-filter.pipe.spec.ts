import { FundRequestFilterPipe } from './fund-request-filter.pipe';

describe('FundRequestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FundRequestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
