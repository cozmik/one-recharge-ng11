import { WalletTransactionsPipe } from './wallet-transactions.pipe';

describe('WalletTransactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletTransactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
