import { FundRequestHistoryFilterPipe } from './fund-request-history-filter.pipe';

describe('FundRequestHistoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FundRequestHistoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
